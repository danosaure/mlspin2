export const CSV_OPTIONS = { separator: '|' };

// Counties we want to keep
export const COUNTIES = new Set(['Suffolk', 'Norfolk', 'Middlesex', 'Essex', 'Bristol', 'Plymouth']);

export const TOP_AGENTS = 200;
